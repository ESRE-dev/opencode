# Ordered manifest of local/* branches for the sync cycle.
# rebuild-local-dev.sh merges these in listed order.
# Lines starting with # are ignored. Blank lines are ignored.
#
# Ordering rationale:
#   - foundational/infra patches first (cancel, subagent, watchdog, tool, provider)
#   - compaction stack as a sequential block (intra-stack dependencies)
#   - features in the middle (skill-preamble, tui-navigation)
#   - docs/misc last (least likely to conflict)
#
# Cross-branch dependencies (surfaced by 2026-04-27 catch-up Phase 1):
#   - local/compaction-todo depends on local/compaction-agent-identity
#     (consumes its buildIdentityReinforcement helper)
#   - local/compaction-todo's watchdog/spawn.ts edits depend on
#     local/intelligent-session-watchdog being in place
#
# DROPPED branches:
#   - local/compaction-thinking-signature: removed 2026-06-10 (cycle
#     opencode-k4t) as OBSOLETE. Upstream PR #25303 (29ec07700c) now
#     strips reasoning parts on model mismatch in message-v2.ts:373-380
#     (live path) and compact-v2 to-llm-message.ts. Branch ref retained;
#     archived at tag archive/compaction-thinking-signature-2026-06-10.
#
#   - local/intelligent-session-watchdog: RETIRED 2026-06-10 (cycle
#     opencode-k4t; was briefly DEFERRED, then evaluated and retired).
#     3 of its 4 pillars are covered elsewhere: stuck-tool detection +
#     4h task deadline by local/tool-timeout, transport stalls by
#     upstream chunkTimeout/headerTimeout (#29837/#29484). The unique
#     remainder (LLM-investigated stuck-session recovery) was never
#     reachable (watchdog tools were never registered in
#     tool/registry.ts) and its code targets deleted upstream modules.
#     If the pain recurs, rebuild small as a PLUGIN (tool.execute hooks
#     + event stream) — see bd opencode-k4t.4. Ref retained locally;
#     archived at tag archive/intelligent-session-watchdog-2026-06-10.
#
# CLEAN-vs-MANUAL verdicts (Phase 1 of 2026-04-27 catch-up) are recorded
# in specs/2026-04-27-upstream-catchup/plans/local-<name>.md and the
# bd epic opencode-dby.
#
# 2026-06-10 catch-up (cycle opencode-k4t, +2060 commits onto ab701d20eb):
# fresh obsolescence verdicts + per-branch rebase plans live in bd epic
# opencode-k4t (children .1-.13).

local/cancel-propagation
local/subagent-hardening
local/tool-timeout
local/provider-fallback
local/compaction-agent-identity
local/compaction-todo
local/skill-preamble
local/tui-navigation
local/build-command
local/docs
local/misc
