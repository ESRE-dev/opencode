.DS_Store
node_modules
.worktrees
.sst
.env
.env.local
.idea
.vscode
.codex
*~
playground
tmp
dist
ts-dist
.turbo
**/.serena
.serena/
**/.omo
.omo/
/result
refs
Session.vim
/opencode.json
a.out
target
.scripts
.direnv/

# Local dev files
opencode-dev
UPCOMING_CHANGELOG.md
logs/
*.bun-build
tsconfig.tsbuildinfo

# Scratch investigation notes
scratch/

# Beads / Dolt files (added by bd init)
# Whole .beads/ tree is per-clone state — config, Dolt DB, and JSONL
# backups all stay local. Backup goes to S3 via Dolt auto-push, never
# git. See meta:BD_SETUP.md for the policy.
.beads/
.dolt/
*.db
.beads-credential-key

# Machine-local AI agent config
.claude/
CLAUDE.md
